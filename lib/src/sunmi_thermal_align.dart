class SunmiThermalAlign {
  final int value;
  const SunmiThermalAlign._internal(this.value);
  static const left = SunmiThermalAlign._internal(0);
  static const center = SunmiThermalAlign._internal(1);
  static const right = SunmiThermalAlign._internal(2);
}
